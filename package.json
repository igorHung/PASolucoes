{
  "name": "docker_builder",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "cbuild": "heroku container:push web",
    "cdeploy": "heroku container:release web",
    "cbd": "heroku container:push web && heroku container:release web",
    "logs": "heroku logs --tail"
  }
}
